'use client'

import { FOREGROUND_COLOR } from '@/lib/theme'
import styled from 'styled-components'

export const SiteTitleWrapper = styled.h1`
  a {
    font-size: 1.6rem;
    color: ${FOREGROUND_COLOR};
    text-decoration: none;
  }
`
